import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
  root: {
    height: "100%",
  },
  shiftContent: {
    paddingLeft: 260,
  },
  content: {
    height: "100%",
  },
}));

export default useStyles;
